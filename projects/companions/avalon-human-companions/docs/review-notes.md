# Review Notes

## Implementer self-review

- Diff limited to intended files: yes; `mods/avalon-human-companions/` plus `docs/hotkey-registry.md` for the new default panel hotkey.
- Research/design notes updated: yes.
- No generated binaries or game files included: yes; build output is covered by `.gitignore` and not listed by `git status --untracked-files=all`.
- Dangerous file/network/process behavior absent or justified: yes; 0.1.10 has no save, quest, dialogue, persistence, conversion of existing NPCs, or global faction behavior. It has one default-off per-instance ally proof for a plugin-owned spawned actor, default-off native quick commands attached only to that actor, a default-off proof panel that calls the same bounded command backend, a gated panel scanner trigger that calls the existing scanner, lifecycle diagnostics/guardrails for the active proof actor, a proof-only Hold runtime movement block for the active proof actor, panel input/focus locking while visible, proof-panel size/button-state polish, and an offline human NPC diagnostic map generator. File writes are limited to BepInEx config generation plus explicit scanner/lifecycle CSV outputs under the plugin-owned config folder and repo-local generated diagnostic map files under `mods/avalon-human-companions/docs/generated`.
- Hot-path performance checked: yes; `Update()` returns unless a configured hotkey, panel cursor/input guard, or explicitly enabled command-maintenance tick is active, and `LateUpdate()` returns unless the safe proof spawn command, ally proof command, or panel input guard is enabled.
- Validation notes updated: yes.
- First proof notes updated: yes; `docs/research/human-npc-proof-step-2026-06-15.md` records the local `TG.Main.dll` evidence and keeps behavior blocked.

## Code review

- Reviewer: Codex self-review using `docs/code-review-standard.md`.
- Status: 0.1.6 source review found no blocking findings for the proof-panel scanner trigger. The change is limited to one `Scan Actors` button in the existing proof panel, gated by `ActorScanner.EnableDisabledActorScanner`, and calls the existing CSV-only `HumanActorScanner.WriteSnapshot` path. Build/deploy passed with 0 warnings and 0 errors, and local/live DLL hashes match. Runtime scanner-button validation now partially passes: 0.1.6 loaded, the panel opened, and the scanner wrote all three CSV files. The capture did not include the reviewed proof actor because the scan ran before the proof actor was created.

## Known blockers

- Human NPC companion runtime behavior beyond the one-session native ally proof is blocked until exact actor, faction, interaction UI, transition, and save/load behavior is researched and validated.
- The safe proof spawn command is not companion behavior. It must remain explicit, default-off, non-unique-template-only, not-saved, and one-spawn-per-session by default.
- The disabled scanner is implemented and live config is set to `ActorScanner.ScannerHotkey=F6`. Because the hotkey path did not produce CSV output or a scanner log line, 0.1.6 adds a gated panel scanner trigger. The scanner-button write path is validated.
- The one-session native ally proof is not recruitment or persistence. It must remain explicit, default-off, non-unique-template-only, not-saved, and one-spawn-per-session by default.
- The proof panel is approved only as proof UI for the active one-session proof actor. Command behavior passed user smoke testing, and 0.1.5 fixes the known layout/readability issue in code, but the panel is not release-ready until the 0.1.6 layout and scanner trigger are loaded in game and revalidated. The latest screenshot is useful combat-context evidence, but the latest scanner CSV selected `Spec_EnemyZombie_T1_Classic` and still did not include the reviewed proof actor GUID. Version 0.1.8 approves proof-only Hold as a runtime movement lock; true Stay/Wait and saved hold positions remain blocked until researched and validated.
- Diagnostic tool boundary: `mods/Tainted-Diagnostic Tool` is the release/public package folder. Diagnostic development and gate evidence should use the dev folder `mods/template-diagnostics` unless the current task explicitly promotes a tested change into the public package.
- Proof actor live capture gate: passed on 2026-06-15 with a logging caveat. Dev FOA-Diagnostic Tool dump `20260615-193847` was a playable-scene dump with `heroAvailable=true`, `NpcHeroPetAlly=1`, and `NpcHeroSummon=1`. The Avalon Human Companions scanner output included the reviewed target GUID with `markedNotSaved=true`, `hasNpcElement=true`, `hasAlive=true`, `hasHeroPetAlly=true`, `behaviorApproved=false`, and `panelApproved=false`; all six command dry-run rows stayed `blocked=true` and `liveAction=false`. The current log excerpt did not include a manual `F3` trigger line, so the trigger provenance remains a caveat even though the dump and CSV content satisfy the evidence gate.
- Next blocker: proof actor lifecycle/no-persistence. Dismiss, transition/long-move, and save/quit/reload must prove no duplicate, no unmanaged leftover, and no persisted human proof ally before more behavior or stronger command claims are approved.
- Version 0.1.7 implementation review: lifecycle diagnostics/guardrails are scoped to the active proof actor only. They keep the actor marked not saved, discard it if it loses the expected managed proof marker, clear tracked state after Dismiss/discard, and write plugin-owned `human-proof-lifecycle.csv` evidence rows with `behaviorApproved=false` and `persistenceApproved=false`. Build/deploy validation passed; live lifecycle validation remains required.
- Version 0.1.8 implementation review: Hold is scoped to the active proof actor only. It uses a plugin-owned not-saved `ICanMoveProvider` on the proof actor's `NpcElement`, skips follow catch-up while Hold is active, removes the movement block on Follow, Come Close, Defend, Dismiss, or invalid-state guard discard, and does not approve true Wait, persistence, or existing NPC conversion. Build/deploy validation passed with 0 warnings and 0 errors; in-game Hold validation remains required.
- Version 0.1.9 implementation review: panel flow polish is UI/status over existing proof commands only. The input lock reuses the proven Harmony/Rewired pattern from Avalon Companions and FoA Mod Manager, and is active only while the proof panel is visible. The diagnostic map generator reads existing dev FOA-Diagnostic Tool CSVs and keeps every approval column false. Build/deploy validation passed with 0 warnings and 0 errors. `LogOutput.log` confirmed 0.1.9 load and input-lock patch application; user-reported in-game focus/input validation passed. Independent screenshot/video capture is still not available.
- Version 0.1.10 implementation review: size/button-state polish is IMGUI presentation only. It increases the proof panel dimensions, left column width, text sizes, and explicit button visual states, and clears IMGUI control focus after clicked actions. It does not change command availability, proof actor lifecycle, scanner output, input lock behavior, save behavior, or persistence. Build/deploy validation passed with 0 warnings and 0 errors; local/live DLL hashes match. In-game 0.1.10 visual validation remains required.
- Version 0.1.12 implementation review: native prompt bridge is interaction routing only. It adds one runtime-only `Companion` prompt to the active managed proof actor when both native-command and proof-panel gates are enabled, opens the existing proof panel, and removes the old separate quick command actions from that prompt surface. It does not add commands, recruitment, persistence, existing-NPC conversion, dialogue, quest/crime state, custom target selection, true Wait, saved hold positions, or release-ready human companion behavior. Release build passed with 0 warnings and 0 errors; in-game native prompt validation remains required.
- Version 0.1.13 implementation review: default-value change only. It makes `HumanCommands.EnableNativeCommandActions` and `HumanCommandPanel.EnableProofCommandPanel` default `true` for newly generated configs, while proof spawn, scanner output, research-mode override, defend assist, recruitment, persistence, existing-NPC conversion, dialogue, quest/crime state, true Wait, and saved hold positions remain separately gated or blocked. Existing BepInEx configs are not overwritten by this default change. Release build passed with 0 warnings and 0 errors; clean config generation and in-game native prompt validation remain required.
- Version 0.1.14 implementation review: UI surface change only. It suppresses the visible centered IMGUI proof window and opens a plugin-owned Unity UI host matching Avalon Companions' vanilla-style layout: transparent full-screen blocker, right-side choices, and bottom dialogue text band. Choices route to the existing proof backend and do not add recruitment, persistence, existing-NPC conversion, story graph dialogue, custom targeting, true Wait, or saved hold positions. Release build passed with 0 warnings and 0 errors; in-game visual/native-prompt validation remains required.
- Version 0.1.15 implementation review: route correction only. Native NPC `Companion` prompt now opens the companion-style dialogue surface, while `HumanCommandPanel.TogglePanelHotkey` opens a compact Avalon Companions Debug-style IMGUI control panel. The two surfaces have separate visibility state and share only existing cursor/input scope handling. Proof command behavior and safety boundaries are unchanged. Release build passed with 0 warnings and 0 errors; in-game split validation remains required.
