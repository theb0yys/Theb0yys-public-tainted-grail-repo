# Avalon Broodmother Companion Validation Plan

## Static checks

- Release build succeeds for `src/AvalonBroodmotherCompanion.csproj`.
- Source contains the current Broodmother LocationTemplate/NpcTemplate GUID pair: `527d142b35e81a648b6e84ea921ac543` / `7623d4729979e814ea64992d9a399e17`.
- Source contains the current Spider LocationTemplate/NpcTemplate GUID pair: `bfcd6db0f66afc14c94fcb6a9dbde4e7` / `5f36e2717dc68e04091fe7b0aa8bafe0`.
- Source contains exactly the six custom spell template GUIDs recorded in `docs/spider-family-six-summons-gate-2026-08-08.md`: `b7d0d4e6c0de4bb0a000000000000001` through `b7d0d4e6c0de4bb0a000000000000006`.
- Source contains the native Wolf's Call source GUID: `3bd577472a0191c44bf298a82553cf3b`.
- Source contains the native Magic Summon Ally graph GUID: `1ed1718ce1b64c747a329eb4269f529b`.
- Source contains the known Tier 1 vendor GUID: `75a071140bc819d4ab6e9e37abfdfa59`.
- Source uses `TryResolveWorldBroodmotherTemplate` and `TryResolveWorldSpiderTemplate`.
- Source has no raw Spider/Broodmother template fallback, random Spider companion path, or native jump-state claim. Spider AI source is limited to the guarded one-session companion path and the source-only Avalon AI package fixture boundary.
- Source has no `LocationSpawner`, `GroupSpawner`, `AutoGuardSpawningAttachment`, route row, population row, recipe registration, or custom summon template registration path.
- Custom spell source inspection proves native `Wolf's Call` is cloned into the six new template GUIDs and that runtime skill-reference rewrite is scoped to those custom GUIDs only.
- Custom spell tooltip inspection proves the cloned templates rewrite `lightCastInfo` and `heavyCastInfo` to selected-companion `Summon` descriptions, with no inherited wolf-command text.
- Custom spell icon inspection proves `ItemIconComponent.Refresh` override is scoped only to the six custom call items, embeds `broodmother-current-redblack.jpg`, `broodmother-reserved-pale.jpg`, `broodmother-reserved-gold.jpg`, `spider-small-skin1-redblack.jpg`, `spider-small-skin2-pale.jpg`, and `spider-small-skin3-gold.jpg`, uses the matching embedded image and an independent definition-keyed sprite cache entry for all six variants, retains all six row sprites concurrently, and falls back without an external icon file or hard dependency.
- Embedded audio inspection proves nine spider PCM WAV resources are compiled into `AvalonBroodmotherCompanion.dll`, every embedded WAV passes strict RIFF/WAVE PCM validation, audio playback uses the game-shipped `FMODUnity` reference, and the source contains no native audio attachment mutation, FMOD Studio bank/event replacement, or shared audio registry edit.
- Custom spell grant is disabled by default.
- Merchant stock insertion defaults to enabled, targets only `Shop_Vendor_Tier1`, uses `ShopUI.OnFullyInitialized()`, adds all six custom call items with stock stacking disabled, logs a post-loop six-definition count, and has duplicate guards.
- Every `SpawnLocation` path is guarded by `AllowRuntimeSummon` for runtime hotkey/panel summon, `Hero.Current`, exact selected-definition template validation, pre-spawn `RepetitiveNpcAttachment` visual readback, immediate `MarkedNotSaved`, missing/unique NPC discard, and native ally marker setup. Spell-triggered summons skip only the runtime-hotkey safety gate and still use the same one-session path.
- Spider-family lifecycle source inspection confirms the active actor sets and reads back `KeepCorpseAfterDeath=true`, requires native `DeathElement`, removes `HideHealthBar`, removes `HeroSummonInvisibility`, re-enables AlivePrefab colliders every tick, registers enabled actor colliders with native `HealthElement` hitboxes, temporarily moves registered objects to FoA's `Hitboxes` layer, requires initialized `PlayAnimationDeathBehaviour` linked from the active `DeathElement`, requires `KeepBody=true`, clears only the documented `_alivePrefab` death-visibility suppression target, restores runtime hitbox/layer changes during cleanup, bypasses native summon-friendly-fire prevention only for hero damage to the active companion, keeps a dead/discarded live NPC in a pending native-death handoff state instead of immediately clearing the location, accepts death only through exact selected-definition native `NpcDummy` plus `Corpse` handoff, and retains dead-body material references after accepted same-session death.
- Spider-family combat bridge inspection confirms event-free ShortBite/LeapJump commits apply damage only after native target handoff succeeds, only to live non-hero targets inside the documented range, only through native `HealthElement.TakeDamage(Damage)`, and with the LeapJump visual sidecar limited to successful bridge damage.
- Recall, dismiss, swap, failure cleanup, and shutdown cleanup set `MarkedNotSaved`.
- Hotkey registry review confirms runtime summon, recall, defend, dismiss, and panel hotkeys remain default-disabled, while the active companion menu uses the runtime-only native `Companion` prompt plus a scoped `E` native-interact recovery path only when the active companion is near and camera-focused.
- Native prompt inspection confirms the Broodmother mod owns one direct `Companion` action, removes stale native quick-command fallback actions from earlier builds, synchronizes/removes the action through active companion lifecycle paths, activates only the active not-saved companion actor's native `LocationInteractability` while the prompt is attached, restores the original interactability during cleanup, reports active/not-interactable/syncing state in the HUD, mirrors the Avalon Companions 11-choice native command-dialogue menu process, routes Unity `Button.onClick` and manual hit-test selection through the same command dispatcher with only same-frame duplicate suppression, keeps the manual direct pointer fallback copied from the proven Avalon Companions path (`Input.GetMouseButtonDown(0)`, one frame guard, real `Input.mousePosition`, visible enabled row hit-test, and direct dispatch through the same command handler, with no Broodmother-specific submit-release or pointer-release state), keeps non-terminal command choices open after dispatch so selected mode/range and status text can resync, exposes no attack button or target selector, does not run follow catch-up/combat AI ticks while the menu is visible, treats Keep Close / Keep Pace / Keep Distance as spacing-only commands, uses the proven Avalon Companions cursor/input scope order while the menu is open (Tainted Interface `BeginCustomUiScope` by reflection first, FoA Mod Manager `SetCustomUiScope` by reflection fallback), keeps native `ForceCursorVisibility` plus local real-cursor capture/reassert/restore active while visible, reasserts the cursor/host/sync/manual-hit-test path from the `OnGUI` pass while visible, does not draw a Broodmother-owned virtual cursor, keeps hover/manual direct-click fallback on the real Unity pointer position, and contains no Avalon Companions bridge/API dependency, hard FoA Mod Manager reference, or hard Tainted Interface reference.
- Run `dotnet run --project mods/avalon-broodmother-companion/ai-package/tests/AvalonBroodmotherCompanion.AI.Package.V1.Fixtures/AvalonBroodmotherCompanion.AI.Package.V1.Fixtures.csproj -c Release` and confirm the Contracts-only AI package manifest, safety constants, goal policy, role/position rules, attack rules, commit rules, Runtime V2 registration, and assembly-boundary checks pass.

## Live validation

Runtime/save validation is not covered by static build validation. A later live gate must use a throwaway save and check plugin load, all six custom spell template registrations, known Tier 1 merchant stock insertion for all six items, inventory tooltip cast rows for each item, Broodmother variant item/HUD/dialogue icon rendering, smaller Spider variant item/HUD/dialogue icon rendering, embedded spider audio startup, summon/recall call cue audibility, combat attack cue audibility, follow walking cue audibility, disabled-default no-spawn behavior, configured summon/swap, spell grant, spell cast for each of the six items, hostility, recall, dismiss, duplicate prevention, save-exclusion readback, transition behavior, shutdown cleanup, command logs with `targetSource`, `targetRegistered`, `currentTargetMatched`, `npcInCombat`, `healthHitboxes`, `healthHitboxAdds`, `hitboxLayerChanges`, and `damageBridge`, native `Companion` prompt rendering, HUD `Native menu: Companion active`, command-dialogue menu open without immediate accidental choice dispatch, no Broodmother-owned yellow-square/virtual cursor artifact, menu hover/click using the real cursor, menu choice interaction through the copied mouse-down direct hit-test without unexpected recall/reset, visible LeapJump bridge VFX, and BetterSummon compatibility.

The next lifecycle-focused live check must also prove `BROODMOTHER_COMPANION_LIFECYCLE_READY`, visible/native health bar behavior after `HideHealthBar` removal, hero hits reducing active companion health, active companion ShortBite/LeapJump commits reducing enemy health through `BROODMOTHER_COMPANION_DAMAGE_BRIDGE`, enemy hits reducing active companion health when applicable, `BROODMOTHER_COMPANION_NATIVE_DEATH_PENDING` while FoA has not yet produced the dummy/corpse, retained same-session body through native `BROODMOTHER_COMPANION_NATIVE_DEATH_ACCEPTED`, no magenta material loss after death, and no unintended damageability change for other native summons.

The first live gate is defined in `docs/live-validation-gate-2026-08-06.md`. It remains blocked for FoA launch, throwaway-save use, config arming, runtime commands, and log inspection until separately authorized.
