# Avalon Broodmother Companion Research

Status: six custom spider-family spell items, known merchant stock, and guarded one-session companion cast-intercept implementation slice; static build passed, live recast validation not run.

Current governing evidence:
- `mods/avalon-broodmother-companion/docs/commercial-stack-broodmother-ai-integration-packet-2026-08-09.md` defines the under-evaluation six-role Rabbit/GOAP/PlayMaker/Blaze/Runtime V2 target and offline acceptance contract. It is planning/evaluation material only; implementation remains blocked behind independent promotion and the exact `BlazeOwned` custom Spider actor lifecycle gate. It does not authorize FoA launch, deployment, save access, or release claims.
- `mods/avalon-broodmother-companion/docs/blazeowned-spider-lifecycle-gate-2026-08-09.md` is the source-confirmed offline lifecycle gate for creation, native-AI suppression, exclusive binding, damage/death ownership, interruption, restoration, and idempotent disposal. It rejects current Spider `BlazeOwned` eligibility pending exact native-owner decompilation because the accepted Blaze lifecycle only owns newly created Avalon actors while the current companion remains a native `Location`/`NpcElement` with active FoA AI, movement, combat, health, and death owners. It authorizes no implementation, deployment, FoA launch, save access, or release claim.
- `mods/avalon-broodmother-companion/docs/tg-main-spider-ownership-construction-decision-2026-08-09.md` completes the exact offline `TG.Main.dll` native-owner decompilation requested by that lifecycle gate. It finds no complete reversible takeover boundary across summon/ally logic, AI state, combat behavior, movement, controller/root motion, Animancer, health reactions, and death; rejects native-shell/Blaze-motion; and selects wholly Avalon-created actors for future `BlazeOwned` roles. It also records a source contradiction in the current summon death-acceptance predicate. The decision authorizes no runtime implementation: a reviewed full custom Spider component contract remains required.
- `mods/avalon-awakened/docs/research/broodmother-first-s2-implementation-packet-2026-08-06.md` reserves the exact mod identity, resolver route, explicit-enable config, one-session lifecycle, native ally setup, structural checks, and blocked behaviors.
- `mods/avalon-awakened/docs/research/broodmother-first-s1b-a-wolfs-call-burning-ember-trace-2026-08-06.md` proves `Wolf's Call` is `ItemTemplate_Magic_Tier1_SummonWolf`, GUID `3bd577472a0191c44bf298a82553cf3b`, with `Magic_Summon_Ally` graph `1ed1718ce1b64c747a329eb4269f529b` and `SummonPrefab` item override.
- `docs/research/frameworks/tainted-content-import-process-2026-08-05.md` records native `ItemTemplate` cloning, private `TemplatesLoader.AddToMap` insertion, `TemplatesProvider` lookup, `World.Add(new Item(...))`, and hero grants as source-confirmed implementation patterns.
- `mods/tainted-lockpick/src/Patches/CustomLockpickTemplateRegistry.cs`, `mods/tainted-lockpick/src/Plugin.cs`, and `mods/food-drink-asset-proof/src/Patches/CustomItemNativeEffectPatch.cs` provide local precedent for custom item-template clone registration, hero grant, and custom-item-only skill-reference rewrites.
- `mods/realistic-longsword/README.md` records the known merchant target `75a071140bc819d4ab6e9e37abfdfa59` / `Shop_Vendor_Tier1` and the merchant-stock route: `ShopUI.OnFullyInitialized()` prefix, decompressed `RestockableStock`, `World.Add(new Item(...))`, then `Stock.AddItem(...)`.
- `mods/realistic-longsword/src/Plugin.cs` implements the duplicate-safe known-merchant route with one-session stock keys, existing shop-template checks, decompressed `RestockableStock`, and visible-after-add logging.
- `mods/avalon-awakened/src/AvalonCreatureWorldApi.cs` exposes the public Broodmother and Spider resolvers used by this slice.
- `mods/avalon-awakened/src/BroodmotherCi4ControlledActorRuntime.cs` records exact Broodmother template and NPC template identity.
- `mods/avalon-broodmother-companion/docs/spider-family-six-summons-gate-2026-08-08.md` is the owning gate for the 0.1.28 spider-family summon slice: it cites the canonical custom-creature process, the current Broodmother and Spider CI4 runtime identities, public resolver routes, selected `skin1-1`/`skin2-1`/`skin3-1` visual roots, merchant-stock route, spell intercept route, lifecycle/audio route, and source-only AI authority widening.
- `mods/dragon-knight-companion/src/Plugin.cs` and `mods/avalon-companions` provide precedent for one-session command, not-saved location handling, native summon faction, `NpcHeroPetAlly`, recall, dismiss, and guarded defend behavior.
- `mods/avalon-awakened/docs/research/ci5e-world-elephant-combat-death-name-correction-2026-07-16.md` records that `LocationTemplate.SpawnLocation(..., string overridenLocationName, ...)` feeds `Location.DisplayName`, and `VCEnemyHealthBar.StartPointing` reads `location.DisplayName`.
- `mods/origins-of-avalon/docs/research/hook-candidate-methods.generated.tsv` records `NpcElement.get_Health`, `NpcElement.get_HealthElement`, `NpcElement.get_HealthStat`, and `NpcElement.get_MaxHealth`; `mods/avalon-awakened/src/CreatureCombatBehaviourSystem.cs` and related controlled-actor proofs use `npc.AliveStats.Health.Percentage`.
- `mods/avalon-companions/docs/research/custom-dialogue-interface-2026-06-19.md` and `mods/avalon-companions/docs/research/native-dialogue-decision-gate-2026-06-21.md` keep the supported route as a runtime-only native `Companion` prompt opening a plugin-owned command UI, while true `VDialogue` / `StoryBookmark` remains blocked.
- Support bundle `20260806-135149Z-4e92624b` captured `Broodmother's Call` throwing `NullReferenceException` during native `Magic_Summon_Ally` / `SkillSpawnLocation`, making the direct native Broodmother `SummonPrefab` rewrite a rejected cast route.
- Installed `TG.Main.dll` decompilation confirms `ItemTemplate` owns private `lightCastInfo` and `heavyCastInfo` `MagicItemTemplateInfo` fields; `MagicItemTemplateInfo` owns `magicType`, `effectType`, and `magicDescription`; `ItemTooltipMagicStatsComponent` renders the visible light/heavy cast rows from those fields.
- Installed `TG.Main.dll` decompilation confirms `ItemIconComponent.Refresh(Item, View, ItemDescriptorType)` reads `Item.Icon`, while `mods/food-drink-asset-proof/src/Patches/CustomItemIconPatch.cs` provides the proven GUID-scoped Harmony prefix pattern for custom item icon display.
- `mods/avalon-awakened/ai-package/src/AvalonAwakened.Spiders.AI.Package.V1/AvalonAwakenedSpiderAiV1Contract.cs` defines the spider combat thresholds and animation boundary: ShortRange state `16`, `Spider_Attack1_ShortRange_CI4`, `Spider_Attack2_ShortRange_CI4`, `Spider_jump`, `Spider_jump_v2`, no direct native calls, no save writes.
- `mods/avalon-awakened/ai-package/tests/AvalonAwakened.Spiders.AI.Package.V1.Fixtures/Program.cs` verifies that leap attacks resolve to the secondary ShortRange clip and that jump clips remain host motion clips until a native jump state is proven.
- `mods/avalon-awakened/host/src/AvalonAwakened.Spiders.Host.V1/AvalonAwakenedSpiderHostV1.cs` translates FoA spider facts into bounded host commands without package native calls or save writes.
- Runtime reflection against installed `TG.Main.dll` confirmed `NpcElement.NpcAI` and `NpcAI.EnterCombatWith(ICharacter, bool)` are public, enabling a target-specific native combat handoff from the companion host.
- Installed `TG.Main.dll` decompilation confirms `NpcHeroSummon.Init()` marks the NPC as a hero summon, adds `HideHealthBar` to the summon location, adds `HeroSummonInvisibility` when the hero is not in combat, captures AlivePrefab colliders and enables them only while `_allyInCombat`, and listens to `HealthElement.Events.TakingDamage` to prevent hero friendly fire when the current hero setting blocks damage to summons. The Broodmother companion route therefore must explicitly undo those summon side effects for the active Broodmother only.
- Installed `TG.Main.dll` decompilation confirms `HealthElement.Init(...)` initializes its `HitboxWrapper` from `AliveComponent.hitboxes`, while public `HealthElement.AddHitbox(Collider, in HitboxData)` and `RemoveHitbox(Collider)` support runtime hitbox registration. This means enabled Unity colliders alone do not prove a spawned actor is wired into native damage.
- Installed `TG.Main.dll` decompilation confirms `HealthElement.TakeDamage(Damage)` is the native damage entry point and runs dealing/taking hooks, hitbox checks, damage modifiers, health reduction, damage dealt/taken events, and death routing. `Damage.GetNpcDmgBasedOnDamageType(...)`, `DamageParameters.Default`, and `RawDamageData(float, ...)` provide the bounded native construction path for Broodmother-owned fallback damage.
- Installed `TG.Main.dll` decompilation confirms NPC melee damage normally depends on attack-release animation events flowing through `WeaponEventsListener` / `EnemyBaseClass` to the current combat behaviour. `mods/avalon-awakened/docs/generated/spider-ci4-animation-mapping-finalized-2026-08-03.json` records the Broodmother ShortRange clips as event-free, so target handoff can succeed without native outgoing hit release events.
- `mods/avalon-awakened/docs/research/ci5-native-logic-lifecycle-contract-2026-07-15.md`, `mods/avalon-awakened/docs/research/ci5c-creature-combat-behaviour-system-2026-07-15.md`, `mods/avalon-awakened/src/AvalonCreatureWorldApi.cs`, and `mods/avalon-awakened/src/SpiderCi4ControlledActorRuntime.cs` establish the native lifecycle contract: set/read back `KeepCorpseAfterDeath=true`, require native death behavior, preserve native `DeathElement` ownership, and accept death only through `NpcDummy` plus `Corpse` on the same location.
- User-supplied source folder `D:\downlods\Development folder\development assets\asset storage\c&c\AUDIO\creature\spider` contains nine MP3 spider creature files: one walking cue, one call cue, and seven attack cues. `ffprobe` read them as MP3 streams: walking `4.320000` seconds at 48 kHz stereo, call `58.440000` seconds at 24 kHz stereo, and each attack cue `3.024000` seconds at 48 kHz stereo.
- `mods/creature-combat-sfx-proof/README.md` records the proven audio sidecar boundary: native FMOD Studio playback is preserved, raw FMOD Core sidecars do not replace native events, and the runtime does not mutate templates or infer arbitrary native families. `mods/creature-combat-sfx-proof/src/ActorAudioInjectionRuntime.cs` provides the local precedent for validating PCM WAV payloads, creating in-memory FMOD Core sounds with `OPENMEMORY | CREATESAMPLE | LOOP_OFF | 3D`, setting attenuation distances and volume, tracking active voices, and releasing sounds.

Current implementation:
- BepInEx GUID: `kane.tgfoa.avalon-broodmother-companion`.
- BepInEx name: `Avalon Broodmother Companion`.
- Assembly: `AvalonBroodmotherCompanion.dll`.
- Custom spell items:
  - `Broodmother's Call - Asha, Crimson Vanguard`, template `ItemTemplate_Magic_Tier1_AvalonBroodmotherCall`, GUID `b7d0d4e6c0de4bb0a000000000000001`, target `Broodmother Asha - Crimson Vanguard`.
  - `Broodmother's Call - Velra, Pale Pouncer`, template `ItemTemplate_Magic_Tier1_AvalonBroodmotherCallSkin2`, GUID `b7d0d4e6c0de4bb0a000000000000002`, target `Broodmother Velra - Pale Pouncer`.
  - `Broodmother's Call - Aurex, Gilded Spitter`, template `ItemTemplate_Magic_Tier1_AvalonBroodmotherCallSkin3`, GUID `b7d0d4e6c0de4bb0a000000000000003`, target `Broodmother Aurex - Gilded Spitter`.
  - `Spider's Call - Rook, Crimson Harrier`, template `ItemTemplate_Magic_Tier1_AvalonSpiderCallSkin1`, GUID `b7d0d4e6c0de4bb0a000000000000004`, target `Rook - Crimson Harrier Spider`.
  - `Spider's Call - Vesper, Pale Ambusher`, template `ItemTemplate_Magic_Tier1_AvalonSpiderCallSkin2`, GUID `b7d0d4e6c0de4bb0a000000000000005`, target `Vesper - Pale Ambusher Spider`.
  - `Spider's Call - Nox, Gilded Finisher`, template `ItemTemplate_Magic_Tier1_AvalonSpiderCallSkin3`, GUID `b7d0d4e6c0de4bb0a000000000000006`, target `Nox - Gilded Finisher Spider`.
- Custom spell display descriptions say the spell calls the selected allied Avalon spider-family companion, keeps one spider-family companion active, and replaces the current companion on recast.
- Custom spell light/heavy cast tooltip rows are rewritten on the cloned templates to `Summon` with selected-companion summon/replace descriptions, avoiding inherited Wolf's Call command text.
- Custom spell item icons are overridden for the exact six custom GUIDs through `ItemIconComponent.Refresh`; Broodmother variants use the embedded red/black, pale, and gold Broodmother JPGs first, and smaller Spider variants use the supplied embedded red/black, pale, and gold smaller Spider JPGs first. All six variants then fall back to Tainted Interface by reflection with `GetItemIcon(custom spell GUID)` and `GetIcon(companion.creature)`, then to the plugin-owned procedural spider `Texture2D`/`Sprite`. Runtime sprites and embedded icon textures are released on plugin teardown, plugin-owned fallback textures are destroyed, and no hard Tainted Interface dependency is required.
- Custom spell source: native `Wolf's Call`, GUID `3bd577472a0191c44bf298a82553cf3b`.
- Custom spell runtime graph: native `Magic_Summon_Ally` clone retained for item shape, but exact custom item casts are intercepted on `Skill.Perform()` and routed through the guarded one-session Broodmother companion summon path.
- Legacy direct `SummonPrefab` rewrite defaults to disabled.
- Runtime hotkey/panel summon is disabled by default.
- Spell-triggered companion summon is enabled by default for the exact six custom spell items.
- Spell item registration is enabled by default.
- Spell item grant is disabled by default because it is save-visible.
- Merchant stock insertion is enabled by default for `Shop_Vendor_Tier1` only, quantity `1` for each of the six custom call items; cloned custom call items are inserted without stock stacking and the route logs a post-loop six-definition stock count.
- Runtime summon, recall, defend, dismiss, and optional panel hotkeys default to no key; the companion menu entry is the native `Companion` prompt. Live 0.1.10 evidence showed the action could synchronize while FoA did not start it, so 0.1.11 adds a scoped native-interact recovery path that uses the same `E` interaction key only when the player is near and camera-focused on the active Broodmother.
- The resolver is called by name and fails closed if Avalon Awakened or the selected exact public method is unavailable.
- The implementation has no raw template fallback.
- The companion spawn passes the selected definition display-name override to `LocationTemplate.SpawnLocation(...)` and validates `Location.DisplayName` before accepting the actor.
- Before spawn, the selected `RepetitiveNpcAttachment` is assigned the selected exact visual address and read back. Broodmother variants use the current Broodmother CI4 template and `5.5` visual scale multiplier; smaller Spider variants use the current Spider CI4 template and `5.5` visual scale multiplier after the live Vesper check showed the previous `3.0` multiplier produced an undersized runtime companion.
- The companion lifecycle surface now sets and reads back `NpcElement.KeepCorpseAfterDeath=true`, requires native `DeathElement`, removes `HideHealthBar` from the active runtime location, removes `HeroSummonInvisibility` from the active NPC, re-enables AlivePrefab colliders every maintenance tick, registers enabled actor colliders with the active NPC's `HealthElement`, temporarily moves registered collider objects to FoA's `Hitboxes` layer, requires at least one registered native health hitbox after controller readiness, requires initialized `PlayAnimationDeathBehaviour` linked from `DeathElement` with `KeepBody=true`, clears only the documented `PostponedRagdollBehaviourBase._alivePrefab` suppression target before lifecycle ready, logs `BROODMOTHER_COMPANION_LIFECYCLE_READY` only after animated death visibility is ready, bypasses native summon-friendly-fire prevention only for current-hero damage to the active companion, restores runtime hitboxes/layers on cleanup, and accepts death only through `BROODMOTHER_COMPANION_NATIVE_DEATH_ACCEPTED` when the same location contains exact selected-template `NpcDummy` plus `Corpse`. Live 0.1.10 evidence rejected the earlier wolf-shaped layer-24 non-trigger `BoxCollider` assumption for Broodmother because the runtime actor reported no such hitboxes while still carrying native colliders; 0.1.15 replaces that weak collider-only proof with explicit `HealthElement` hitbox registration. Live 0.1.16 death feedback showed an empty Broodmother health bar, magenta body presentation, and no native-death accepted log row; source review found dead/discarded active NPC paths cleared the companion and destroyed Broodmother runtime material clones before the native dummy/corpse handoff could be observed. Live 0.1.18 log review still showed `animatedDeath=0` while the lifecycle was incorrectly logged as ready; the current route treats that as waiting, applies the same reviewed alive-prefab suppression correction used for visible no-ragdoll Death(44), treats dead/discarded live NPC state as native death pending, closes command/audio surfaces, keeps the same-session not-saved location tracked, logs `BROODMOTHER_COMPANION_NATIVE_DEATH_PENDING`, and only clears active command state after the exact dummy/corpse handoff is accepted while retaining death visual material references until plugin teardown.
- Advanced spider companion AI is enabled by default for native-recognized live combat candidates. It selects targets in priority order from `Hero.PossibleAttackers`, Broodmother `NpcElement.PossibleAttackers`, then Broodmother `NpcElement.PossibleTargets`, measures distance and facing angle when the target exposes coordinates, classifies ShortBite, LeapJump, Feint, Reposition, or targeted native combat through the spider thresholds, and prompts native `NpcAI.EnterCombatWith(target, forceChange: true)` plus `NpcHeroPetAlly.EnterCombat()`. Mid-range `Reposition` now drives native combat handoff instead of becoming a package `Hold`, while ShortRange 16 remains limited to valid bite/leap release windows. Immediate target pressure is enabled by default and pushes the same native target handoff every `0.2` seconds while a live native combat candidate exists, even before the spider package attack commit window opens. Runtime command logs include `targetSource`, candidate count, native target registration, current-target match, and `NpcAI.InCombat` readback so live tests can distinguish target detection from accepted native combat state. The runtime tick and advanced combat prompt defaults are `0.35` seconds, with migration from the previous `0.75` / `1.25` defaults.
- Because the generated CI4 ShortRange clips are event-free, ShortBite and LeapJump commits also apply the active companion native damage bridge after target handoff succeeds. The bridge skips non-damage handoffs, skips the hero, requires target spatial data and documented range, builds normal physical melee `Damage` from the active companion NPC melee stat, calls `target.HealthElement.TakeDamage(damage)`, logs `BROODMOTHER_COMPANION_DAMAGE_BRIDGE`, and spawns a transient plugin-owned green/gold LeapJump arc VFX on successful leap/ranged bridge damage.
- LeapJump classification records ShortRange 16 and `Spider_Attack2_ShortRange_CI4`, but does not force `Spider_jump` or `Spider_jump_v2` because the spider AI fixture keeps those as motion-only until a native jump state is proven.
- Source-only AI package `mods/avalon-broodmother-companion/ai-package/src/AvalonBroodmotherCompanion.AI.Package.V1` defines the spider-family Avalon AI Contracts V2 package boundary for native-recognized live combat candidate pressure, role selection, positioning, one attack slot, telegraph, targeted native handoff, ShortRange 16 handoff, and fail-closed stop. It accepts only the exact current Broodmother identity or exact current Spider identity. The package references `AvalonAI.Contracts.V2` only; it does not reference Unity, BepInEx, Harmony, FoA assemblies, runtime execution modules, or the live gameplay plugin.
- The live Broodmother companion plugin compiles `BroodmotherCompanionAiTypes.cs` and `BroodmotherCompanionAiV1Contract.cs` as linked source and calls `BroodmotherCompanionAiV1Contract.Evaluate(...)` for combat decisions. The linked source is compiled without Avalon AI Contracts symbols, so the plugin adds no Avalon AI runtime/package assembly dependency.
- `Dialogue.EnableNativeCompanionPrompt` defaults to true and synchronizes one direct runtime-only native `Companion` `AbstractLocationAction` on the active verified Broodmother. The `Companion` prompt opens a Broodmother-owned Unity UI companion menu that mirrors the Avalon Companions native command-dialogue process: Follow, Hold Position, Defend, Keep Close, Keep Pace, Keep Distance, Come Close, Recall, Recover, Dismiss, Leave, selected mode/range highlights, bottom listening/status text, and the embedded current Broodmother portrait, with Tainted Interface `hud.companion-icon-background` when available. Local `TG.Main.dll` decompilation confirms `Location.Interactable` is backed by `LocationInteractability` and `Location.SetInteractability(LocationInteractability.Active)` is the native setter used by visual scripting; the Broodmother prompt now stores the active actor's original interactability, sets only that runtime not-saved Broodmother instance to `Active` while the prompt is attached, restores the original value on prompt cleanup, and reports HUD status as active only when the actor is actually interactable. `mods/avalon-core/docs/decisions/0037-shared-custom-ui-layer-contract.md` and `mods/foa-mod-manager/src/FoAModManagerApi.cs` establish `FoAModManagerApi.SetCustomUiScope(ownerId, active, freezeWorld)` / `SetControllerCursorScope(ownerId, active)` as the shared cursor/input ownership surface. `mods/Tainted Interface/src/TaintedInterfaceApi.cs` and `mods/Tainted Interface/src/Plugin.cs` establish `TaintedInterfaceApi.BeginCustomUiScope(ownerId, freezeWorld)` as the shared renderer-facing wrapper over FoA Mod Manager custom UI scope plus `EnsureInteractiveCursor()`. The proven Avalon Companions menu path in `mods/avalon-companions/src/Patches/PetCompanionController.cs`, `mods/avalon-companions/src/Patches/PetCompanionController.DialogueUi.cs`, `mods/avalon-companions/src/Patches/TaintedInterfaceBridge.cs`, and `mods/avalon-companions/src/Patches/FoAModManagerBridge.cs` opens the dialogue UI through Tainted Interface scope first, falls back to FoA Mod Manager custom UI scope, captures/restores the real cursor state, reasserts cursor visibility/unlock while visible, uses Unity `Button.onClick`, and keeps the manual fallback as `Input.GetMouseButtonDown(0)` against the real `Input.mousePosition`. `mods/crime-and-consequences/src/Plugin.cs` provides the local native cursor precedent by attaching `Awaken.TG.Main.UI.Cursors.ForceCursorVisibility` to the current hero while its popup is open. Live feedback after 0.1.25 showed the menu still registered hover but did not accept choices; 0.1.26 replaces the failed Broodmother Mod-Manager-first route with the proven Avalon Companions scope order and cursor loop while leaving command dispatch unchanged. Live feedback after 0.1.20 showed a small Broodmother-owned virtual cursor artifact and unusable options, so the current route keeps that visible virtual cursor removed and returns hover/manual direct-click fallback to Unity's real `Input.mousePosition`, matching the validated Avalon Companions Unity UI dialogue path. Live feedback after 0.1.21 showed row hover registering but choices not being accepted, while the command log showed a new menu open with no following `dialogue-choice` row; the current route keeps the proven Avalon Companions manual fallback exactly: `Input.GetMouseButtonDown(0)`, one frame guard, real `Input.mousePosition`, visible enabled row hit-test, and direct dispatch through the same command handler, without Broodmother-specific pointer-release state. Live 0.1.24 feedback showed the game loaded `0.1.24` and opened the command UI, but no `dialogue direct choice clicked` or `dialogue-choice` line followed; source comparison then found Broodmother still kept a separate submit-release/debounce gate on the Unity `Button.onClick` route that Avalon Companions does not have. The current route keeps that Broodmother-specific gate removed: Unity `Button.onClick` and manual hit-test selection both run the same command dispatcher, leaving only same-frame duplicate suppression. Live 0.1.15 feedback and logs showed command UI opening while repeated follow catch-up recalls still ran, and the menu range choices also recalled because they passed `recallActive=true`; the current route skips the full companion tick while the menu is visible and makes Keep Close / Keep Pace / Keep Distance non-recall spacing commands. The prompt surface is tracked as active, marked not saved, removes stale quick-command action stacks from earlier builds, is removed on invalid/dismiss/shutdown paths, and does not add attack buttons, target selectors, an Avalon Companions bridge, Avalon Companions interop, story graphs, bookmarks, template edits, native quick-command action stacks, hard FoA Mod Manager references, hard Tainted Interface references, or save-owned interactions.
- Native companion menu command dispatch now keeps non-terminal choices open after applying their command, immediately resynchronizing selected mode/range highlights and bottom listening/status text. Only `Leave` and `Dismiss` close the menu.
- `Audio.EnableEmbeddedSpiderAudio` defaults to true. The plugin embeds nine converted PCM WAV resources under `AvalonBroodmotherCompanion.Audio.Spider.*`, validates each WAV before creating an FMOD Core sound, and plays 3D one-shot cues through the game-shipped FMOD runtime. The call lane is wired to successful summon and recall with a 60 second default cooldown because the supplied call source is 58.44 seconds long. The attack lane is wired to successful native combat handoff from defend assist, immediate target pressure, and advanced spider AI. The walk lane is wired to follow-distance ticks while the companion is active, not holding, not defending, and not already in a combat handoff. Active voices are stopped when the companion is cleared or embedded audio is disabled, and sounds are released on plugin teardown. The implementation does not mutate native actor/item audio attachments, FMOD Studio events, native templates, Wolf's Call audio, Avalon Awakened assets, or shared audio registries.
- `docs/live-validation-gate-2026-08-06.md` defines the first live validation route and keeps deployment, FoA launch, save access, runtime spawn, defend-assist validation, BetterSummon compatibility, packaging, and icon work blocked until separately authorized.

Still blocked:
- Native spellbook UI editing, native localization table editing, shared Tainted Interface companion icon catalog packaging, recipe injection, custom summon template registration, and the second `skin1-2`/`skin2-2`/`skin3-2` source roots.
- All-merchant distribution, loot distribution, reward distribution, recipe distribution, and merchant-specific economy balancing.
- Random spawning, population rows, route rows, scene hooks, spawner hooks.
- Broodmother asset copying, porting, rebuilding, or redeployment.
- User-supplied Spider/Broodmother source-image assignment and shared catalog promotion.
- Shared/native audio bank editing, native actor-audio selector replacement, and source MP3 redistribution outside the embedded Broodmother plugin assembly.
- Direct forced jump clip playback, actor restoration, auto-respawn, and save-owned companion behavior.
- Avalon AI runtime/host package execution remains blocked until a separate live/host gate authorizes it. This slice uses linked source evaluation inside the Broodmother plugin only.
- True native `VDialogue`, `StoryBookmark`, story graph, `DialogueAttachment`, `PetTalkAttachment`, `StoryInteractAction`, vanilla serialized interaction-list edits, and Avalon Companions interop remain outside this slice.
- FoA launch, save access, live recast claims, live summon claims, grant persistence claims, and public packaging until separately authorized.
