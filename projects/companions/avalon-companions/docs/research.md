# Avalon Companions Research

## Baseline evidence

- `Research/Making Tainted Grail The Fall of Avalon Mods-deep-research-report.md` establishes the repo's practical route: PC Mono branch, BepInEx v5 Mono, `netstandard2.1` C# plugin DLLs, and Harmony/HarmonyX patches.
- `Research/Tainted Grail The Fall of Avalon modding research for combat crime sleep and spawn systems-deep-research-report.md` marks spawn and respawn work as realistic but risky when it moves into patrol generation, random encounters, persistence, bosses, uniques, or quest-spawned NPCs.
- `mods/living-avalon/docs/research.md` and `mods/living-avalon/docs/research/spawn-targets.md` document that vanilla actor spawning is location-template based and that NPC presence/unique-NPC systems should not be bypassed.
- `mods/living-avalon/docs/research/contextual-spawn-diagnostics-2026-06-14.md` documents that `NpcAttachment.IsUnique`, `RepetitiveNpcAttachment`, `UniqueNpcAttachment`, `IAlive`, `Location.Template`, `Location.Coords`, and parent spawner context can support diagnostics.
- `mods/living-avalon/docs/population-map.md` and `mods/living-avalon/docs/pool-validation-plan.md` require runtime evidence, unique/story exclusions, and save/load validation before any actor template can drive behavior.
- `mods/avalon-companions/docs/research/pet-and-summon-targets-2026-06-14.md` documents local `TG.Main.dll` evidence for built-in pet, summon, and ally systems.
- `mods/avalon-companions/docs/research/runtime-diagnostics-2026-06-14.md` records the first successful in-game diagnostic run: no active pet/summon models, `CommonReferences.PetBaseVariant.IsSet=true`, four Qrko pet templates, and a GUID-backed CSV.
- `mods/avalon-companions/docs/research/pet-creature-shortlist-diagnostics-2026-06-14.md` records the diagnostics-only path for more pet/creature `LocationTemplate` candidates using scene spawner evidence, regular/non-abstract filters, and uniqueness evidence.
- `mods/avalon-companions/docs/research/wolf-bear-native-ally-marker-2026-06-15.md` records local `TG.Main.dll` evidence for applying the game's native `NpcHeroPetAlly` marker to the two plugin-owned wolf/bear one-session candidates.
- `mods/avalon-companions/docs/research/wolf-bear-native-defend-assist-2026-06-15.md` records local `TG.Main.dll` evidence for waking the native `NpcHeroPetAlly` combat path when the hero has attackers.
- `mods/avalon-companions/docs/research/animal-roster-expansion-2026-06-15.md` records the reviewed live diagnostic evidence for deer, pig, cow, and bullrat one-session roster candidates.
- `mods/avalon-companions/docs/research/offensive-creature-roster-expansion-2026-06-15.md` records the reviewed FOA-Diagnostic Tool 0.4.3 `creature_templates.csv` evidence for the expanded animal, offensive creature, and undead one-session roster candidates.
- `mods/avalon-companions/docs/research/transition-save-lifecycle-gate-2026-06-15.md` records the 0.1.2 transition/save gate: conservative cleanup plus lifecycle diagnostics only, with no persistence claim.
- `mods/avalon-companions/docs/research/advanced-command-behavior-2026-06-15.md` records the 0.1.3 command-behavior gate: Heel, follow range profiles, and command CSV logging only, with no custom attack, target selection, persistence, squads, or humanoid companions.
- `mods/avalon-companions/docs/research/heel-placement-safety-hotfix-2026-06-15.md` records the 0.1.4 placement hotfix for the close command: visible `Come Close` label, no below-normal close offset for creature candidates, and native position verification before recall moves.
- `mods/avalon-companions/docs/research/native-quick-commands-2026-06-15.md` records the 0.1.5 native quick-command layer, 0.1.6 panel-bridge correction, 0.1.7 cursor correction, 0.1.8 dismiss-reset correction, and 0.1.9 command-menu default: one runtime-only native `Companion` prompt opens the Avalon command menu, while rotating quick commands remain fallback-only.
- `mods/avalon-companions/docs/research/advanced-native-ally-behavior-2026-06-15.md` records the 0.1.10 native ally behavior slice: live hero attacker counts, defend armed/clear logs, throttled native `NpcHeroPetAlly.EnterCombat()` prompts, and richer command audit reasons.
- `mods/avalon-companions/docs/research/command-polish-status-feedback-2026-06-15.md` records the 0.1.11 command polish slice: per-active-companion panel status and clearer Follow/Stay/Defend mode feedback without new behavior paths.
- `mods/avalon-companions/docs/research/lifecycle-validation-checkpoint-2026-06-15.md` records the 0.1.12 lifecycle validation slice: panel lifecycle summary plus manual checkpoint using the existing lifecycle guard and CSV dump.
- `mods/avalon-companions/docs/research/managed-companion-recovery-2026-06-16.md` records the 0.1.13 recovery slice: managed-only invalid/dead/stuck detection plus safe removal or verified recall, with no healing, respawn, persistence, targeting, or custom AI.
- `mods/avalon-companions/docs/research/session-continuity-persistence-gate-2026-06-16.md` records the next gate after recovery: inspect lifecycle/command evidence first, then only metadata/status continuity may be considered; actor persistence, reload restoration, auto-respawn, saved ownership, and new combat commands remain blocked.
- `mods/avalon-companions/docs/research/session-continuity-evidence-review-2026-06-16.md` records live CSV/log inspection for the session-continuity gate: 1504 lifecycle rows, 215 command rows, no `markedNotSaved=false` managed actor rows, no `touchesPersistence=true`, and no `touchesTargeting=true`. This clears only metadata/status continuity for a future slice; actor restoration, reload respawn, saved ownership, and same-session re-adoption after reload remain blocked.
- `mods/avalon-companions/docs/research/metadata-status-continuity-2026-06-16.md` records the 0.1.14 metadata/status continuity slice: plugin-owned remembered companion GUID/name, command mode, follow range, status feedback, and command-log evidence only; no actor restoration, respawn, persistence, or re-adoption.
- `mods/avalon-companions/docs/research/responsive-native-behavior-polish-2026-06-18.md` records the 0.1.15 responsive native polish slice: explicit commands may queue the existing follow and native defend ticks immediately, without clearing per-companion defend prompt cooldowns or adding custom AI, targeting, persistence, attack commands, or restoration.
- `mods/avalon-companions/docs/research/framework-contract-extraction-2026-06-18.md` records the 0.1.16 framework polish slice: extract internal companion command/mode/range/recovery and roster-entry contracts into `AvalonCompanions.Framework` without changing runtime behavior or adding Avalon Core integration yet.
- `mods/avalon-companions/docs/research/core-handoff-design-2026-06-19.md` records the 0.1.17 Core handoff design slice: document the future diagnostic/planning-only Core boundary while keeping Avalon Core optional and avoiding runtime registration or advanced behavior.
- `mods/avalon-companions/docs/research/core-diagnostic-bridge-2026-06-19.md` records the 0.1.18 Core diagnostic bridge slice: read only Avalon Core host trust reports by optional reflection, fail closed, and avoid dependency, adapter registration, companion-profile registration, and advanced behavior.
- `mods/avalon-companions/docs/research/panel-debug-native-dialogue-roadmap-2026-06-19.md` records the 0.1.19 debug-panel roadmap slice: the IMGUI panel is a temporary debug surface, native custom dialogue is the next research gate, and custom AI plus taming/training/loyalty systems remain downstream.
- `mods/avalon-companions/docs/research/custom-dialogue-interface-2026-06-19.md` records the 0.1.20 custom dialogue slice: the runtime native `Companion` prompt may open a plugin-owned command-choice interface, while true native `VDialogue`/story graph dialogue, custom AI, and progression systems remain blocked.
- `mods/Tainted Interface/src/TaintedInterfaceApi.cs` and `mods/Tainted Interface/docs/validation-plan.md` record the shared UI Layer runtime contract: optional reflection-safe style access, cursor helper, and custom UI scope backed by FoA Mod Manager. The current live proof has embedded dark-fantasy textures and a validated consumer path, while screenshot/video validation remains a release gate for each consumer.
- `mods/avalon-companions/docs/research/custom-dialogue-ui-system-host-2026-06-19.md` records the 0.1.22 correction: 0.1.21 still rendered the player command surface through IMGUI, so the native `Companion` prompt now routes to a Unity UI `Canvas` dialogue host while true vanilla `VDialogue` remains blocked.
- `mods/avalon-companions/docs/research/vanilla-style-dialogue-layout-2026-06-19.md` records the 0.1.23 correction: the 0.1.22 Unity UI host still looked like a centered custom panel, so the host now uses right-side choices plus a bottom dialogue text band while true vanilla `VDialogue` remains blocked.
- `mods/avalon-companions/docs/research/shared-icon-consumer-proof-2026-06-19.md` records the 0.1.24 UI-only consumer proof: the debug panel may display shared Tainted Interface companion icons by stable IDs while companion behavior remains unchanged.
- `mods/avalon-companions/docs/research/dialogue-button-input-gate-2026-06-20.md` records the 0.1.25 input correction: the Unity UI dialogue may allow Rewired reads through for EventSystem button clicks while the IMGUI debug panel keeps strict Rewired neutralization.
- `mods/avalon-companions/docs/research/dialogue-direct-click-gate-2026-06-20.md` records the 0.1.27 direct-click correction: visible Unity UI dialogue choices may use a dialogue-local pointer hit-test fallback while command behavior remains unchanged.
- `mods/avalon-companions/docs/research/per-companion-icon-mapping-2026-06-20.md` records the 0.1.28 UI-only mapping correction: the debug panel maps existing roster families to expanded Tainted Interface companion icon IDs while behavior remains unchanged.
- `mods/avalon-companions/docs/research/companion-hud-icon-overlay-2026-06-20.md` records the 0.1.29 UI-only HUD badge slice: normal gameplay may show the active managed companion icon over a curated Dragon background while behavior remains unchanged.
- `mods/avalon-companions/docs/research/post-0.1.29-development-sequence-2026-06-20.md` records the post-0.1.29 gate order: 0.1.30 command-surface polish, native dialogue decision, lifecycle validation, gated Core diagnostic/profile registration, then custom AI research.
- `mods/avalon-companions/docs/research/command-surface-polish-2026-06-20.md` records the 0.1.30 command-surface polish slice: visual command states, consistent close routes, and stronger dialogue audit rows without behavior expansion.
- `mods/avalon-companions/docs/research/companion-hud-icon-overlay-2026-06-20.md` also records the 0.1.31 HUD placement hotfix: raise the top-left badge above the Wyrd Scent/status overlay lane without behavior changes.
- `mods/avalon-companions/docs/research/custom-ai-boundary-map-2026-06-20.md` records the first advanced-feature research gate: native follow/pathing/combat/targeting boundaries are mapped, but custom AI, target overrides, taming, training, loyalty, and persistence remain blocked pending a read-only diagnostic probe and runtime validation.
- `mods/avalon-companions/docs/research/ai-boundary-evidence-review-2026-06-21.md` records the 0.1.34 evidence review: the first live AI boundary CSV set had 276 rows, native combat-adjacent evidence, and zero unsafe mutation/Core flags. It approves only a future non-mutating profile/intent classifier design, not AI execution.
- `mods/avalon-companions/docs/research/companion-ai-profile-intent-audit-2026-06-21.md` records the 0.1.35 implementation boundary: runtime-only `CompanionAiProfile` / `CompanionAiIntent` contracts plus a default-off `companion-ai-profile.csv` audit are allowed; custom AI execution, target overrides, movement overrides, persistence, progression systems, and Core-executed behavior remain blocked.
- `mods/avalon-companions/docs/research/ai-profile-audit-smoke-review-2026-06-21.md` records the 0.1.36 smoke review: 0.1.35 loaded with the profile audit enabled, 27 profile rows were reviewed, active `Sharg Candidate` rows classified `MovementBlocked` and `IdleNativePatrol`, and all mutation/Core flags stayed false.
- `mods/avalon-companions/docs/research/ai-profile-check-smoke-aid-2026-06-21.md` records the 0.1.37 smoke aid: the debug panel may expose `AI Profile Check`, writing one immediate `panel-ai-profile-check` row only when the profile audit config is enabled and otherwise logging a blocked audit row.
- `mods/avalon-companions/docs/research/profile-driven-native-assist-2026-06-21.md` records the 0.1.38 behavior bridge: when explicitly enabled, `CompanionAiIntent` may trigger only existing recall/catch-up and native hero-pet ally defend paths.
- `mods/avalon-companions/docs/research/profile-driven-native-assist-hardening-2026-06-21.md` records the 0.1.39 hardening pass: profile-driven catch-up/defend actions defer the matching legacy follow/defend tick to avoid immediate duplicate action rows.
- `mods/avalon-companions/docs/research/profile-driven-native-assist-smoke-fix-2026-06-21.md` records the 0.1.40 smoke fix: while the profile-driven gate is enabled, the profile loop owns the existing catch-up/defend prompting paths so legacy ticks cannot claim the first action before profile audit rows are written.
- `mods/avalon-companions/docs/research/native-dialogue-decision-gate-2026-06-21.md` records the 0.1.41 native dialogue decision: no safe real `VDialogue` / `StoryBookmark` route is proven, so the plugin-owned Unity UI remains the supported companion dialogue surface.
- `mods/avalon-companions/docs/research/lifecycle-validation-gate-2026-06-21.md` records the 0.1.42 lifecycle validation gate route: throwaway-save transition, rest, quit/reload, return, duplicate cleanup, orphan cleanup, and not-saved evidence without changing lifecycle behavior.
- `mods/foa-mod-manager/docs/research.md` and `mods/foa-mod-manager/docs/design.md` document the cursor and input-lock pattern needed for plugin-owned IMGUI panels: force cursor visibility, disable Unity input modules, reset axes, and patch FoA/Rewired input reads while the panel is open.
- `Research/Tainted Grail The Fall of Avalon Template and Safe Population Injection Research-deep-research-report.md` establishes that GUID is the canonical template key, `LocationTemplate` is the safe unit for vanilla-style spawning, and runtime dumps should classify candidates without treating them as approved spawn targets.

## Target state

- Game version: Unknown locally; baseline repo research references current Mono workflow as of June 2026.
- Branch: Mono.
- BepInEx track: v5 Mono.
- Assembly expected for future inspection: `TG.Main.dll`.
- Selected companion target: existing pet/summon systems. The approved behavior target is a pet/animal companion roster using the four validated `Spec_Pet_Qrko*` `LocationTemplate` GUIDs plus manually reviewed one-session animal/creature rows.
- Current implementation target: plugin load/config scaffold, disabled-by-default pet/summon and GUID-backed template diagnostics, an opt-in Qrko pet companion roster, opt-in animal/offensive-creature/undead one-session candidates using the native hero pet ally marker plus native attack-response assist where the actor can support it, a conservative lifecycle guard/dump for transition/save validation, safe managed-command ergonomics with Come Close/Heel, follow range profiles, command CSV logging, one runtime-only native `Companion` prompt opening a plugin-owned vanilla-style Unity UI custom dialogue host with dialogue-specific EventSystem input pass-through/direct-click fallback, a separate temporary IMGUI debug panel with optional per-companion shared icons from Tainted Interface, optional Tainted Interface/FoA Mod Manager input scope, throttled native defend prompting with attacker-state audit rows, managed recovery for dead/invalid/stuck active companions, metadata/status-only continuity for remembered companion selection, mode, and follow range, responsive native refresh scheduling after explicit companion commands, extracted internal framework contract types for future Core routing, default-off native AI boundary diagnostics, a default-off read-only companion AI profile/intent audit classifier, and a manual debug-panel profile-audit smoke button.

## Candidate research areas

- Companion actor creation or reuse.
- Follow/pathing behavior.
- Combat allegiance and hostile filtering.
- Summon/follower-adjacent systems.
- Teleport or catch-up behavior after area transitions.
- Save/load and actor persistence.
- Recruitment, dismissal, and dialogue routes.

These are search areas, not approved targets.

## Implementation boundary

- Version 0.1.3 may load, bind config, run diagnostics, run the Qrko pet companion roster when all companion gates are enabled, expose reviewed animal/creature rows as one-session candidates, run the companion lifecycle guard/dump, and expose Heel plus Close/Normal/Far follow range commands for managed roster actors.
- Template candidate dumps are taxonomy evidence only. The four validated Qrko pet GUIDs are approved only for the pet companion roster.
- The reviewed `ReviewQueueNonUniqueSpawnerBacked` animal, offensive creature, and undead rows may be exposed in the roster as one-session creature candidates. They may be spawned only by explicit player command, marked not saved, converted through the native summon-faction plus `NpcHeroPetAlly` path, tracked only in memory, recalled/catch-up moved, dismissed, and prompted to enter native ally combat when `Hero.Current.PossibleAttackers` contains a live attacker. Passive animals may not have useful combat capability. The command panel may expose an explicit `Defend` mode for this same native entry path, while `Companions.EnableNativeDefendAssist` remains the automatic fallback gate outside explicit `Defend` mode. If the native ally marker cannot be applied, the spawn must be discarded.
- Pet/creature shortlist dumps are diagnostics evidence only. A `shortlistCandidate=true` row is not roster approval and does not set `safeSpawnCandidate=true`.
- Do not call `LocationTemplate.SpawnLocation` automatically, on load, or outside the explicit pet companion roster.
- Do not instantiate, clone, despawn, persist, teleport, recruit, or command actors outside the explicit pet companion roster.
- Do not modify NPC presence, unique NPC stash state, quest state, story flags, global faction ownership, or save data. The only approved faction change is the per-instance native summon-faction override used with `NpcHeroPetAlly` on plugin-owned one-session creature candidates.
- Do not use named, unique, boss, story, quest, challenge, summon, debug, tutorial, or scene-critical templates.
- Pet companion spawns must be marked not saved. Runtime controls may adopt active pets only when their template GUID is one of the four validated Qrko roster GUIDs.
- Runtime controls must not apply custom targeting, custom attack commands, defend hotkeys, persistence, or save-owned companion behavior to any one-session creature candidate until those gates are researched and validated.
- The 0.1.3 command log may append `companion-command-log.csv` rows for command decisions. Command rows are audit evidence only and must keep new advanced commands marked as not touching targeting or persistence.
- Version 0.1.4 may hotfix close placement by using native position verification and by keeping one-session creature candidates at normal-or-far tuned offsets. It must not add healing, custom pathing, target selection, attack commands, persistence, or active squads.
- Version 0.1.9 may attach one runtime-only native `Companion` action to managed one-session companions and use it to open the Avalon command menu. Rotating quick-command action elements remain fallback-only when `Companions.EnableNativeCommandMenu=false` and `Companions.EnableNativeQuickCommands=true`. It must not add story graphs, dialogue attachments, template edits, save-owned interaction lists, target selection, attack commands, healing, persistence, or squads.
- Version 0.1.10 may count live hero attackers, log defend armed/clear state, throttle repeated automatic native `NpcHeroPetAlly.EnterCombat()` prompts per managed companion, and add richer command CSV reasons for defend and catch-up decisions. It must not scan arbitrary targets, force hostility, add manual attack commands, add custom AI/pathing, heal actors, persist companions, or create squads.
- Version 0.1.11 may show per-active-companion command-panel status and clearer Follow/Stay/Defend feedback. It must not add commands, target lists, target selection, attack buttons, healing, persistence, squads, or humanoid behavior.
- Version 0.1.12 may show lifecycle summary text and add a panel-only `Lifecycle Check` diagnostic button that runs the existing lifecycle safety pass with `forceDump=true`. It must not restore companions, respawn companions, persist companions, create squads, or add new travel hooks.
- Version 0.1.13 may add a managed-only `Recover` command. It may run the lifecycle guard, discard dead or structurally invalid managed one-session actors when the lifecycle safety guard is enabled, and recall live managed actors beyond the Far catch-up threshold through the existing verified placement path. It must not heal, resurrect, respawn, restore after reload, persist companions, create squads, select targets, add attack commands, or command unmanaged actors.
- The lifecycle guard may remove untracked one-session roster allies and excess active roster actors to prevent transition/reload orphans and duplicates. It must not respawn companions on load or create persistent companion records.
- The next session-continuity slice must inspect 0.1.12 and 0.1.13 lifecycle/command CSV evidence before code changes. It may only consider metadata-only continuity, status feedback, and explicit player-commanded re-summon through the existing one-session path. It must not restore actors after reload, auto-respawn, change `MarkedNotSaved` ownership, save native command actions, or add new combat commands.
- Version 0.1.14 adds metadata/status continuity only: remembers last selected companion GUID/display name, command mode, and follow range in plugin-owned config; shows remembered companion status when no active managed actor exists; and logs metadata/status decisions with `touchesPersistence=false`. It must not spawn, restore, re-adopt, save, or persist actors except through the existing explicit Summon/Swap command path.
- Version 0.1.15 adds responsive native behavior polish only: explicit summon/swap, recall, Come Close, range, mode, recovery, lifecycle-check, and native command entry paths may queue the existing follow catch-up and native defend ticks for immediate evaluation. It must not clear per-companion defend prompt cooldowns, add custom pathing or AI, scan targets, force attacks, add attack commands, persist actors, restore actors, respawn actors, re-adopt actors after reload, or create squads.
- Version 0.1.16 adds framework polish only: internal companion command, mode, range, recovery, and roster-entry contracts may move into `AvalonCompanions.Framework` for future Core handoff. It must not change command routing, actor ownership, native ally behavior, targeting, persistence, UI behavior, or lifecycle behavior.
- Version 0.1.17 adds Core handoff design only: companion-to-Core boundaries and future diagnostic/planning-only descriptor expectations may be documented. It must not add an Avalon Core dependency, register adapters or companion profiles, move runtime behavior into Core, add callbacks, or add advanced companion behavior.
- Version 0.1.18 adds a Core diagnostic bridge only: it may read `AvalonCore.Plugin.TrustReports` by optional reflection and log a read-only summary. It must not add a hard dependency, register adapters or companion profiles, query Core registries, move runtime behavior into Core, add callbacks, or add advanced companion behavior.
- Version 0.1.19 adds debug-panel roadmap polish only: the IMGUI panel may be labelled and gated as debug through `Debug.EnableDebugPanel`, and the native bridge prompt may be labelled `Companion Debug`. It must not add native dialogue/story graph content, fake story bookmarks, custom AI/pathing, target scans, taming, training, loyalty, persistence, restoration, respawn, squads, or new gameplay systems.
- Version 0.1.20 adds a custom dialogue command interface only: the runtime native `Companion` prompt may open a plugin-owned command-choice UI with the approved existing commands visible at once. The debug panel remains separate behind `Debug.EnableDebugPanel`. It must not add native story dialogue, fake story bookmarks, runtime dialogue attachments, template edits, custom AI/pathing, target scans, taming, training, loyalty, persistence, restoration, respawn, squads, or new gameplay systems.
- Version 0.1.21 adds optional shared UI shell integration only: the custom dialogue and debug panel may use Tainted Interface styles, texture-backed panels/buttons, cursor helper, and input-scope acquisition by reflection when that runtime is installed, with the existing local/FoA Mod Manager fallback when absent. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, or move behavior into Core.
- Version 0.1.22 adds a custom dialogue UI host correction only: the runtime native `Companion` prompt may open a plugin-owned Unity UI `Canvas` dialogue host instead of the old IMGUI dialogue window. The debug panel remains IMGUI-only. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, fake native dialogue, or move behavior into Core.
- Version 0.1.23 adds a custom dialogue layout correction only: the Unity UI dialogue host may use right-side command choices plus a bottom dialogue text band instead of the centered panel composition. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, fake native dialogue, or move behavior into Core.
- Version 0.1.24 adds a shared icon consumer proof only: the debug panel may request shared companion icons from Tainted Interface by optional reflection. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, fake native dialogue, copy icon assets, or move behavior into Core.
- Version 0.1.25 adds a dialogue input correction only: Rewired axis/button reads may pass while the Unity UI dialogue is visible so EventSystem-driven choices can click. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, fake native dialogue, or move behavior into Core.
- Version 0.1.27 adds a dialogue direct-click fallback only: the Unity UI dialogue host may manually hit-test enabled visible choice rows and dispatch the existing command delegate. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, fake native dialogue, or move behavior into Core.
- Version 0.1.28 adds a per-companion icon mapping correction only: the IMGUI debug panel may map the selected roster family to expanded Tainted Interface shared icon IDs. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, fake native dialogue, copy icon assets, or move behavior into Core.
- Version 0.1.29 adds a passive gameplay HUD icon overlay only: when an active managed roster companion exists, the gameplay HUD may draw the existing mapped companion icon over `hud.companion-icon-background` from Tainted Interface. It must not add commands, change command routing, alter companion ownership, add targeting, add persistence, add AI/pathing, add taming/training/loyalty, fake native dialogue, copy icon assets, or move behavior into Core.
- Version 0.1.30 adds command-surface polish only: it may improve hover/selected states, disabled-state clarity, close behavior, and command audit rows for the existing Unity UI companion command surface. It must not add commands, change actor ownership, add native story dialogue, add Core-executed behavior, add persistence, add targeting, add custom AI/pathing, or add taming/training/loyalty.
- Version 0.1.31 adds a HUD placement hotfix only: the top-left passive companion badge may be raised above the Wyrd Scent/status overlay lane. It must not change command behavior, actor ownership, targeting, persistence, AI/pathing, Core behavior, or companion lifecycle.
- Version 0.1.32 adds a shared HUD primitive handoff and a default-off read-only AI boundary diagnostic only. The diagnostic may append native AI, movement, unchecked target relation, possible target/attacker count, and movement-blocker state for active managed companions to `companion-ai-boundary.csv`. It must not dispatch commands, change movement, change targets, call target recalculation, call `TargetOverrideElement.GetTarget`, store progression state, persist actors, or execute behavior through Core.
- Version 0.1.33 adds an AI boundary smoke aid only: the debug panel may expose a visible `AI Boundary Check` control; it writes one immediate read-only diagnostic snapshot only when `Diagnostics.WriteCompanionAiBoundaryDiagnostics=true`, and otherwise reports the blocked config state with a blocked command-log audit row. It must not dispatch companion commands, queue native refresh, change movement, change targets, call target recalculation, call `TargetOverrideElement.GetTarget`, store progression state, persist actors, or execute behavior through Core.
- Version 0.1.34 adds AI boundary evidence review and first safe behavior design only. It may document a future runtime-only `CompanionAiProfile` / `CompanionAiIntent` classifier that observes native state and writes audit evidence, but it must not execute behavior, add target overrides, override movement, add attack commands, store progression state, persist actors, or execute behavior through Core.
- Version 0.1.35 adds a runtime-only `CompanionAiProfile` / `CompanionAiIntent` classifier and default-off `companion-ai-profile.csv` audit only. It may classify observed active managed companion state as idle patrol, stay position, follow catch-up candidate, defend waiting, native combat observed, movement blocked, no targets, no active companion, or evidence insufficient. It must not execute behavior, add target overrides, override movement, add attack commands, store progression state, persist actors, or execute behavior through Core.
- Version 0.1.36 adds AI profile audit smoke review only. It may validate BepInEx load, profile-audit config activation, `companion-ai-profile.csv` row output, active managed companion classification, and zero unsafe flags. It must not execute behavior, add target overrides, override movement, add attack commands, store progression state, persist actors, or execute behavior through Core.
- Version 0.1.37 adds an AI profile smoke aid only. The debug panel may expose `AI Profile Check`; when `Diagnostics.WriteCompanionAiProfileAudit=true`, it may write one immediate `panel-ai-profile-check` snapshot using the existing profile audit writer, including a valid `NoActiveCompanion` row when no active managed companion exists. When disabled, it must only report the blocked config state and write a blocked command-log row. It must not dispatch commands, queue native refresh, change movement, change targets, call target recalculation, call `TargetOverrideElement.GetTarget`, store progression state, persist actors, or execute behavior through Core.
- Version 0.1.38 adds profile-driven native assist only. When `Companions.EnableProfileDrivenNativeAssist=true`, the in-memory `CompanionAiIntent` classifier may trigger existing recall/catch-up behavior for `FollowCatchUpCandidate` and existing native `NpcHeroPetAlly.EnterCombat()` prompting for `NativeCombatObserved` when the hero already has live attackers. It must not select targets, override movement states, add target override elements, call target recalculation, add attack UI, store progression state, persist actors, or execute behavior through Core.
- Version 0.1.39 hardens profile-driven native assist only. After profile assist performs catch-up or native defend, it may defer the matching regular follow/defend tick by the existing tick interval to avoid immediate duplicate actions. It must not add new behavior beyond the 0.1.38 native-safe assist lane.
- Version 0.1.40 fixes the profile-driven native assist smoke race only. While `Companions.EnableProfileDrivenNativeAssist=true`, legacy follow and defend ticks stand down so the profile loop owns the existing native-safe catch-up and defend prompting paths. It must not add new behavior beyond the 0.1.38 native-safe assist lane.
- Version 0.1.41 is a native dialogue decision slice only. It may document the plugin-owned Unity UI as the supported companion dialogue surface and keep real `VDialogue` / `StoryBookmark` integration blocked. It must not create story bookmarks, register runtime story graphs, add dialogue attachments, edit templates, write save-owned interaction lists, or change runtime command behavior.
- Version 0.1.42 is a lifecycle validation gate only. It may document the throwaway-save route and record build output. It must not change lifecycle behavior, actor ownership, `MarkedNotSaved` handling, persistence, reload restoration, auto-respawn, re-adoption, custom AI, target selection, taming, training, loyalty, or Core-executed behavior.
- The custom AI boundary map remains research-only for behavior. The next safe advanced step is reviewing more boundary/profile evidence, not custom AI execution.

## Unknowns

- Whether FoA has a safe follower or companion system to reuse.
- Whether summon behavior can be adapted without breaking summon limits, combat rules, or persistence.
- Whether plugin-created actors survive load, area transitions, quit, reload, and return.
- Which non-unique NPC or creature templates are safe for a companion role.
- How to keep a companion from interfering with quests, stealth, crime, interiors, or scripted scenes.

## Validation needed before behavior

- Local decompilation notes for follower-like, summon-like, faction, and actor persistence systems.
- Runtime UnityExplorer evidence for any selected actor path.
- Throwaway-save validation: load, create or attach, follow, combat, transition area, quit, reload, return.
- BepInEx log evidence for plugin load and any future patch registration.

## Current diagnostic step

- Implemented disabled-by-default diagnostics only: log pet-system availability, current pet/summon counts, `CommonReferences.Get.PetBaseVariant` availability, and loaded `Spec_Pet_*` template candidates with canonical GUIDs.
- Implemented disabled-by-default CSV output for loaded `Spec_Pet_*` `LocationTemplate` candidates under the plugin's BepInEx config folder.
- Implemented disabled-by-default CSV output for scene-spawner-backed pet/creature `LocationTemplate` candidates under the plugin's BepInEx config folder.
- Added `docs/research/wolf-bear-diagnostic-review-2026-06-14.md` and `.csv` to preserve the wolf/bear crosscheck result as review evidence, not behavior approval.
- Added `docs/research/actor-control-proof-design-2026-06-14.md` and a default-off actor-control proof diagnostic path that writes candidate/component CSVs plus follow/stay/recall/defend dry-run command decisions. These rows are classification evidence only and do not approve wolf, bear, faction, targeting, persistence, or defend behavior.
- Diagnostics stay off by default and do not spawn, transform, persist, or command actors.
- In-game diagnostics passed on 2026-06-14 and found four regular non-abstract Qrko pet `LocationTemplate` rows.
- Implemented opt-in Qrko pet companion roster with selection, summon/swap, recall, dismiss, follow/stay controls, and explicit panel command modes.
- Added the reviewed animal/offensive-creature/undead rows as one-session creature candidates in the roster. Selecting one and using summon/swap spawns the selected candidate, marks it not saved, applies the native hero pet ally marker, tracks it for this session, and limits Avalon-owned controls to recall/catch-up/dismiss plus explicit Follow/Stay/Defend modes that still use the native attack-response path.
- Mirrored the FoA Mod Manager panel input lock so the companion panel unlocks/restores the cursor and blocks movement, camera, menu hover, and character action reads while open.
- Added the 0.1.2 companion lifecycle guard and `companion-lifecycle.csv` dump for transition, rest, quit/reload, duplicate, orphan, and not-saved validation. Next validation is an in-game lifecycle pass on a throwaway save.
- Added the 0.1.3 advanced command slice: `Heel`, persisted Close/Normal/Far follow range profiles for placement/catch-up behavior, and `companion-command-log.csv` for command audit rows. This still does not add custom target selection, attack commands, persistence, active squads, or humanoid companions.
- Added the 0.1.4 close-command placement hotfix: visible button text is `Come Close`, creature candidates no longer use the shrunken Close placement offset, and recall targets are passed through native placement verification with snap-to-ground enabled.
- Added the 0.1.9 native command-menu default: managed one-session companions receive one runtime-only native `Companion` prompt that opens the Avalon command menu. Direct vanilla `PetTalkAction`, `DialogueAction`, `StoryInteractAction`, and `SPetInteract` conversion remains blocked until valid story graph/bookmark registration and `PetVariantBase` applicability are proven.
- Added the 0.1.10 advanced native ally behavior slice: native defend now tracks hero-attacker state, logs armed/clear transitions, throttles repeated `NpcHeroPetAlly.EnterCombat()` prompts, and records richer command CSV reasons without adding custom target selection or attack commands.
- Added the 0.1.11 command polish slice: the panel reports active companion distance, mode state, native ally marker, native prompt, and not-saved state, and mode feedback explains Follow/Stay/Defend behavior.
- Added the 0.1.12 lifecycle validation slice: the panel reports the last lifecycle pass summary and can force a `panel-lifecycle-check` CSV snapshot through the existing lifecycle guard.
- Added the 0.1.13 managed recovery slice: the panel and fallback native quick-command path can run `Recover`, which removes dead/invalid managed actors under the lifecycle guard and recalls live out-of-range managed actors through existing verified placement.
- Added the 2026-06-16 session-continuity/persistence gate: the next researched step is CSV/log evidence review and metadata-only continuity design, not actor persistence or more combat commands.
- Added the 2026-06-16 session-continuity evidence review: live CSVs support a metadata/status-only continuity slice, but full rest/quit/reload validation and all actor persistence remain blocked.
- Added the 0.1.14 metadata/status continuity slice: remembered companion GUID/name, command mode, and follow range are stored as plugin-owned metadata; the panel/status path reports remembered companions when no managed actor is active; `continuity-status` command-log rows record that manual `Summon / Swap` is required. This still does not restore, respawn, persist, save, or re-adopt actors.
- Added the 0.1.15 responsive native polish slice: explicit companion command paths queue existing follow/defend ticks immediately so native catch-up and native defend evaluation happen on the next update, while per-companion defend prompt cooldowns, no-targeting, and no-persistence boundaries remain unchanged.
- Added the 0.1.16 framework polish slice: extracted internal companion framework contracts are allowed, but behavior remains the existing 0.1.15 native-safe companion path.
- Added the 0.1.17 Core handoff design slice: Avalon Core remains optional, no runtime descriptor is registered yet, and behavior remains the existing native-safe companion path.
- Added the 0.1.18 Core diagnostic bridge slice: optional trust-report reflection logs are allowed, while adapter/profile registration and Core-executed behavior remain blocked.
- Added the 0.1.19 debug-panel roadmap slice: the current IMGUI surface is now a temporary debug panel, the native prompt bridge is `Companion Debug`, and native custom dialogue is the next research/design gate before custom AI, taming, training, or loyalty systems.
- Added the 0.1.20 custom dialogue interface slice: the runtime native prompt is `Companion`, opens a plugin-owned command-choice interface, and keeps the debug panel on `KeypadPeriod`; true native story dialogue and advanced behavior remain blocked.
- Added the 0.1.21 shared UI shell slice: the custom dialogue and debug panel prefer Tainted Interface styles, textures, cursor helper, and shared custom UI scope by optional reflection, while preserving local/FoA Mod Manager fallback and leaving companion behavior unchanged.
- Added the 0.1.22 Unity UI dialogue host correction: the runtime native prompt stays `Companion`, but opens a Unity UI `Canvas` dialogue host instead of an IMGUI dialogue window; true native story dialogue and advanced behavior remain blocked.
- Added the 0.1.23 vanilla-style dialogue layout correction: the Unity UI dialogue host now uses right-side choices and a bottom text band instead of the centered panel layout; true native story dialogue and advanced behavior remain blocked.
- Added the 0.1.24 shared icon consumer proof: the IMGUI debug panel can show the selected roster icon from Tainted Interface when that runtime is installed; missing icons or missing UI Layer keep the panel functional.
- Added the 0.1.25 dialogue input correction: Unity UI dialogue keeps world movement/look blocked but allows EventSystem/Rewired button input so visible options can click.
- Added the 0.1.27 direct-click correction: the dialogue host can dispatch existing enabled choices through a dialogue-local pointer hit-test fallback.
- Added the 0.1.28 per-companion icon mapping correction: existing roster families now request distinct expanded Tainted Interface icon IDs where available, with `companion.creature` retained only as an unmapped fallback.
- Added the 0.1.29 companion HUD icon overlay slice: active managed companions can show a passive top-left or bottom-right HUD badge using the existing mapped icon and Tainted Interface's Dragon background ID.
- Recorded the post-0.1.29 development sequence: 0.1.30 command-surface polish, native dialogue decision gate, lifecycle validation gate, Core diagnostic/profile registration gate, and custom AI research gate.
- Added the 0.1.30 command-surface polish slice: dialogue row states, close routes, and audit rows are clearer while command behavior remains unchanged.
- Added the 0.1.31 companion HUD placement hotfix: the top-left badge is raised above the Wyrd Scent/status overlay lane.
- Added the custom AI boundary map: native ally/pet follow, movement, combat, and target-selection ownership are documented; the next advanced implementation must be a read-only diagnostic probe before any custom AI, target override, taming, training, loyalty, or persistence behavior.
- Added the 0.1.32 companion AI boundary diagnostic: `Diagnostics.WriteCompanionAiBoundaryDiagnostics=false` by default appends `companion-ai-boundary.csv` rows for active managed companions only, records `touchesCommands=false`, `touchesMovement=false`, `touchesTargeting=false`, `touchesPersistence=false`, and `coreExecuted=false`, and avoids native target recalculation plus `TargetOverrideElement.GetTarget`.
- Added the 0.1.33 AI boundary smoke aid: `AI Boundary Check` on the debug panel is visible as a smoke control, writes a single `panel-ai-boundary-check` snapshot only when the AI boundary diagnostic config is enabled, reports the blocked config state when disabled, and does not queue native follow/defend refresh.
- Added the 0.1.34 AI boundary evidence review and first safe behavior design: reviewed 276 live CSV rows with zero unsafe mutation/Core flags and defined only a future non-mutating profile/intent classifier design.
- Added the 0.1.35 companion AI profile/intent audit: `Diagnostics.WriteCompanionAiProfileAudit=false` by default appends `companion-ai-profile.csv` rows that classify observed active managed companion state while recording all mutation/Core flags as false.
- Added the 0.1.36 profile audit smoke review: 27 live profile rows validated `NoActiveCompanion`, `MovementBlocked`, and `IdleNativePatrol` output with zero unsafe mutation/Core flags, while Stay, follow catch-up, Defend waiting, native combat, interior, stealth, transition, rest, quit/reload, and return coverage remain pending.
- Added the 0.1.37 AI profile smoke aid: `AI Profile Check` writes one immediate `panel-ai-profile-check` profile-audit row only when enabled, or a blocked `ai-profile-check` command-log row when disabled.
