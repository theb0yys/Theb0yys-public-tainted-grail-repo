name: Public surface guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  guard:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Reject assets, binaries, archives, large files, and obvious secrets
        shell: pwsh
        run: ./contributing/tools/verify-public-surface.ps1

      - name: Verify documentation links, metadata, and indexes
        shell: pwsh
        run: ./contributing/tools/verify-documentation.ps1
