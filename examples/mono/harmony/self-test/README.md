# Mono Harmony Self-Test

Use this example to prove that BepInEx 5 and Harmony are wired correctly before you patch any FoA method.

It patches a method inside the plug-in itself, so a successful result tells you the patching setup works without risking game behavior.
