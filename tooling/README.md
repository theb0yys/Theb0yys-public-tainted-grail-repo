# Tooling

- [verify-public-surface.ps1](verify-public-surface.ps1) — rejects common binary/archive/asset formats, oversized tracked files, obvious secrets and private machine paths.

The GitHub public-surface workflow runs this guard on pushes and pull requests.
