# Mono Magic Projectile Speed

Use this starter when you want to change player-owned magic projectile speed while leaving targeting and damage native.

The default multiplier is 1.0, so the project can be built and loaded before you deliberately enable a gameplay change.
