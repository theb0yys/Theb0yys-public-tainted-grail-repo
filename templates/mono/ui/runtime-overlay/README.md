# Mono Runtime UI Overlay

Use this starter when you need a small mod-owned IMGUI panel for diagnostics or a focused tool.

It owns only its own window. FoA menus, cursor state, pausing, focus, and controller input remain under their existing owners unless you explicitly integrate shared UI tooling.
