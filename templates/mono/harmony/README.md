# Mono Harmony Templates

Use these templates when you want to add Harmony patching to a Mono/BepInEx 5 mod and first prove the patch lifecycle safely.

Choose the pattern that matches the change: a basic patch, a Postfix that adjusts a returned result, or a guard that blocks one action while preserving the normal path otherwise.
