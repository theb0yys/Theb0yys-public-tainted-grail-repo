# Magic Tweaks — Dual-Runtime Starter

Use this starter for magic tuning that should share configuration and feature rules across runtimes.

Put spell-selection/tuning logic in common code, then use the runtime-specific hosts for the exact FoA projectile, cost, status, or cast hooks required by each build.
