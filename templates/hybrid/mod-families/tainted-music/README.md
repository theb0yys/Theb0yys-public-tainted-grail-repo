# Dual-Runtime Starter

Use this starter for music or ambience features that need shared selection/configuration rules across runtimes.

Keep policy and state shared; each runtime host should own the exact AudioCore/FMOD hooks and lifecycle needed to integrate with FoA audio.
