# Dual-Runtime Starter

Use this starter for a cross-runtime diagnostics tool that collects evidence without mutating gameplay.

Keep report/query logic shared, while each host handles the exact runtime access required to inspect Mono or IL2CPP game state safely.
