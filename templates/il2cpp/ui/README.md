# IL2CPP UI Templates

Use these templates when you need a small IL2CPP runtime overlay or diagnostic screen.

The starter owns only its own panel. FoA's native cursor, modal state, pausing, focus, and controller input remain separate unless you deliberately integrate the shared tooling responsible for them.
